[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[variables]
CI = "false"
GENERATE_SOURCEMAP = "false"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -l $PORT"