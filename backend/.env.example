# Port (Railway fournit automatiquement)
PORT=5000

# URL du frontend (à configurer sur Railway)
FRONTEND_URL=https://your-frontend.railway.app

# Optionnel: taille max des fichiers (en MB)
MAX_FILE_SIZE=100